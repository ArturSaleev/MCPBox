# MCPBox

MCPBox — это control plane на Go для MCP-серверов. Он позволяет группировать локальные и удалённые MCP-backend'ы по проектам, смотреть что они публикуют, отслеживать трафик и отдавать один project URL для Claude, Cursor, Codex, LM Studio и локальных сценариев с Ollama.

<p align="center">
  <a href="images/main.png">
    <img src="images/main.png" alt="MCPBox main view" width="32%" height="150px" />
  </a>
  <a href="images/market.png">
    <img src="images/market.png" alt="MCPBox market view" width="32%" height="150px" />
  </a>
  <a href="images/log.png">
    <img src="images/log.png" alt="MCPBox logs view" width="32%" height="150px" />
  </a>
</p>

<p align="center">
  <sub>Нажмите на любой скриншот, чтобы открыть его в полном размере.</sub>
</p>

## Зачем нужен MCPBox

Когда MCP-серверов становится много, всё быстро превращается в набор JSON-конфигов, терминалов и локальных костылей. MCPBox собирает это в один бинарник и одну встроенную панель управления.

Основные возможности:
- Организация MCP-серверов по проектам
- Единый project endpoint `/mcp/{project_token}`, который агрегирует все включённые серверы проекта
- Встроенная База знаний / RAG-коллекции с локальной индексацией кода, текста, CSV, XLSX, DOCX, PPTX и PDF с текстовым слоем
- Live inspection локальных `stdio` серверов: `tools`, `resources`, `prompts` и соседний README
- Health-check при создании, редактировании, ручной проверке и запуске локального сервера
- Audit log для MCP-трафика и управляющих действий
- Пауза проекта и включение или отключение отдельных серверов
- Встроенный Market / Catalog для синхронизации, установки, проверки и удаления интеграций
- Запуск локального `ollama + MCPBox` в один клик через встроенный `mcphost/sdk`
- Один бинарный файл со встроенным React UI и локальной SQLite-базой

## Что нового в 1.2.1

- Переработана страница Market: каталоговый UI больше не живёт одним большим блоком внутри `App.tsx`
- Добавлен переключатель источника каталога: удалённый URL или локальный JSON-файл
- Источник каталога по умолчанию перенесён с `webeasy.kz` на [mcpbox.sh](https://mcpbox.sh)
- Улучшен UX Market: понятнее install flow, статус установки пакета и видимость использования по проектам
- Добавлено безопасное удаление установленного пакета, если он не используется ни в одном проекте
- `Add to project` показывается только для уже установленных пакетов
- Добавлено дублирование проекта с новым именем и полным клонированием серверов, интеграций, package links и подключённых баз знаний
- Форма создания и редактирования проекта упрощена: поле рабочей директории убрано из модального окна
- В manifest добавлена поддержка `icon_url`
- В manifest добавлена поддержка `system_dependencies` с проверкой системных зависимостей до установки
- В manifest добавлена поддержка `default_env`, `env_schema`, `secret: true` и `env_var`
- Секреты из catalog install form теперь уходят в переменные окружения, а не остаются в видимых аргументах команды
- В Project UI добавлена маскировка чувствительных аргументов команды и секретных env/header значений
- Ошибки синхронизации каталога теперь пишутся в audit log
- В manifest добавлена поддержка `health_check` для проверки интеграции после добавления в проект
- Добавлен MVP для Docker runtime: `runtime.type: "docker"` и `install.strategy: "docker_pull"` для stdio-ориентированных Docker MCP серверов
- Улучшен fallback для Python: установка работает и на системах, где есть только `python3`
- Экран логов теперь включает встроенный performance monitoring: задержки, ошибки, трафик, топ серверов и компактные графики

## Что нового в 1.2.0

- Встроенный workflow Базы знаний / RAG с отдельной вкладкой `Knowledge Base` в UI
- Глобальные knowledge collections, которые можно подключать к одному или нескольким проектам
- Локальная индексация и поиск по коду, тексту, CSV, XLSX, DOCX, PPTX и PDF с текстовым слоем
- Project-level MCP tool `search_project_knowledge` для поиска по подключённым базам знаний через endpoint проекта
- Более прозрачный audit trail для вызовов Knowledge Base и поисковой активности
- Улучшенный документный контекст с секциями вида `Sheet`, `Slide` и `Page`

## Что нового было в 1.1.1

- Исправлен запуск Ollama на Windows: встроенная кнопка `Launch Ollama` теперь корректно стартует через отдельный PowerShell-сценарий
- Надёжнее обработка команд на Windows: пути с кавычками и пробелами больше не ломают запуск
- Исправлен запуск терминала на Windows: чат-сессия теперь открывается в рабочей директории проекта
- Добавлено автоматическое восстановление на Windows: если Ollama daemon ещё не поднят, MCPBox сам запускает `ollama serve` перед открытием чата

## Что нового было в 1.1.0

- Встроенная поддержка Ollama для локального MCP-тестирования в один клик
- Новая страница Market и workflow каталоговых интеграций
- Улучшенный автозапуск проектов, чтобы поднимались все включённые локальные серверы
- Более чистые логи Filesystem MCP в обычной работе

## Быстрый старт

1. Скачайте бинарник для своей ОС из последнего релиза или соберите проект из исходников.
2. Запустите `MCPBox`.
3. Откройте `http://127.0.0.1:38180/`, если браузер не открылся автоматически.
4. Создайте проект.
5. Добавьте локальные `stdio` серверы или удалённые `HTTP streaming` серверы.
6. Скопируйте project endpoint из UI и подключите AI-клиент к `/mcp/{project_token}`.

Для локального тестирования через Ollama:
1. Убедитесь, что `ollama` установлена.
2. Откройте проект, в котором есть хотя бы один включённый MCP-сервер.
3. Выберите локальную модель Ollama в UI.
4. Нажмите `Launch Ollama`.

## Сборка из исходников

Требования:
- Go `1.26+`
- Node.js и npm

Сборка:

```bash
npm --prefix html install
npm --prefix html run build
go build -o MCPBox .
```

Запуск из исходников:

```bash
go run .
```

Порт по умолчанию: `38180`

## Документация

- [README.md](./README.md) - английская пользовательская документация
- [DEVELOPER.md](./DEVELOPER.md) - техническое описание, API и архитектура
- [DEVELOPER-ru.md](./DEVELOPER-ru.md) - русская версия developer guide
- [PRO-ROADMAP.md](./PRO-ROADMAP.md) - roadmap будущей `Pro`-версии и граница редакций
- [PRO-ROADMAP-ru.md](./PRO-ROADMAP-ru.md) - русская дорожная карта `Pro`
- [RELEASE-1.2.0.md](./RELEASE-1.2.0.md) - черновик release notes для версии `1.2.0`
- [RELEASE-1.1.1.md](./RELEASE-1.1.1.md) - черновик release notes для версии `1.1.1`
- [RELEASE-1.1.0.md](./RELEASE-1.1.0.md) - черновик release notes для версии `1.1.0`

## Примечания

- По умолчанию MCPBox использует локальную SQLite-базу.
- Текущая реализация Базы знаний / RAG использует локальный полнотекстовый поиск через Bleve. На этом этапе embedding-индексы не строятся и не требуются.
- Поддержка Docker runtime в `1.2.1` пока сделана как MVP. Продвинутые сценарии вроде compose-оркестрации, кастомных сетей и preset volumes ещё не реализованы полностью.
- Маскировка секретов работает в UI и в catalog install flow. Уже существующие ручные серверы, которые передают секреты прямо в аргументах команды, лучше перевести на env-переменные для полной безопасности на уровне process list.
- Будущие возможности `Pro` вынесены в `PRO-ROADMAP.md` и не являются частью текущей реализации `1.2.1`.
- Кнопка запуска Ollama показывается только если `ollama` установлена на компьютере.
- Inspection доступен только для локальных `stdio` серверов.
